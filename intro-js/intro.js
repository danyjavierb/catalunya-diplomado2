// Espec que define a JS
// ECMAScript

// lenguajes transpilan a JS
// sistema de tipos
// sound type system (mas prometedores)

// Typescript -> Microsoft -> Lo vemos en clase
// coffeeScript -> CS
// Kotlin -> JetBrains -> recomendado
// Dart -> Google -> deberian estar pendientes
// ReasonML -> REscript -> mas interesantes -> deberian estar pendientes
